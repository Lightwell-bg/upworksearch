# Как делался upwork-scout: агенты, решения, расход

Дата: 2026-09-24. Оркестратор и основной исполнитель — Claude Opus 5.5.
Правила работы взяты из вашего промпта и `CLAUDE.md` проекта: Opus сам делает архитектуру и
сложные части, рутину отдаёт `ojc-boilerplate-executor`, перед делегированием и выбором skill
задаёт вопрос Jev, в конце — Codex-ревью.

## 1. Кто что делал

| Исполнитель | Модель | Что делал | Почему он |
|---|---|---|---|
| **Opus 5.5 (я)** | claude-opus-5-5 | Исследовал Jev API, спроектировал архитектуру, написал весь код `upwork_scout/` (17 модулей), `config.yaml`, шаблон отчёта, все HTML-фикстуры, сквозной smoke, живые проверки в Chrome, разобрал все замечания Codex и исправил их | Jev: «архитектура/парсер/скоринг/оркестрация» → `opus-self` (p=1.0); правило проекта «Opus — основной исполнитель» |
| **ojc-boilerplate-executor №1** | Sonnet | Каркас: `.gitignore`, `requirements.txt`, `.env`/`.env.example`, `run.bat`, venv + установка зависимостей, `git init` | Jev → `ojc-boilerplate-executor` (p=1.0); механическая работа, шла в фоне параллельно с моим кодом |
| **ojc-boilerplate-executor №2** | Sonnet | `README_RU.md` и `README.md` по подробному плану из 13 разделов | Jev → `ojc-boilerplate-executor` (p=1.0); я потом проверил и поправил разделы |
| **ojc-boilerplate-executor №3** | Sonnet | Весь набор тестов `tests/test_*.py` (13 файлов + `conftest.py`) + 3 дозапуска для тестов под исправления Codex | Jev → `ojc-boilerplate-executor` (p=1.0); фикстуры я писал сам, так как они кодируют разметку Upwork |
| **Codex** (ревьюер) | gpt-5.6-sol, reasoning high | 4 раунда `adversarial-review` (плюс 1 сорвавшийся запуск) | Ваше требование: обязательное Codex-ревью и закрытие замечаний |
| **Jev** (TypeSafe через OpenRouter) | typesafe/jev-1.13 | Маршрутизация подзадач и выбор skill; в продукте — AI-оценка вакансий | Ваше требование: typed-вопрос Jev перед делегированием/skill |

Что **не** запускалось и почему:
- `ojc-quick-helper` — не нашлось изолированных тривиальных задач, ради которых стоило бы поднимать
  отдельного агента: мелкие правки я делал сам за один вызов.
- `deep-reasoning-advisor` и `codex:rescue` — не понадобились. `CLAUDE.md` проекта отводит
  архитектуру Opus, а Codex — роль только ревьюера (не соисполнителя).

## 2. Расход токенов и время

### Субагенты Claude (данные харнесса из уведомлений о завершении)

| Агент | Запуск | `subagent_tokens` | Вызовов инструментов | Время |
|---|---|---:|---:|---:|
| №1 каркас | 1 | 24 501 | 15 | 1 мин 40 с |
| №2 README | 1 | 95 807 | 26 | 3 мин 22 с |
| №3 тесты | основной (207 тестов) | 232 984 | 103 | 17 мин 50 с |
| №3 тесты | дозапуск 1 (Codex, раунд 1 → 240 тестов) | 309 084 | 42 | 6 мин 28 с |
| №3 тесты | дозапуск 2 (раунд 2 → 246) | 340 963 | 21 | 3 мин 06 с |
| №3 тесты | дозапуск 3 (раунд 3 → 248) | 369 361 | 19 | 3 мин 00 с |
| **Итого** | | **≈ 490 тыс.** (24.5k + 95.8k + 369.4k) | **226** | **≈ 35 мин** |

Как читать `subagent_tokens`: у агента №3 значение растёт с каждым дозапуском
(233k → 309k → 341k → 369k), а число вызовов инструментов в каждом дозапуске своё и уменьшается.
Значит, это **размер контекста агента на момент завершения** (накопительный), а не
оплаченные токены. Реально обработанных входных токенов больше: каждый шаг агента заново читает
контекст, в основном из кеша. Точной оплаченной суммы харнесс не показывает.

### Codex (из журнала `~/.codex/logs_2.sqlite`, только чтение)

| Раунд | Итог | Шагов модели | Макс. контекст | Сумма входа по шагам* | Время |
|---|---|---:|---:|---:|---:|
| 1 | 5 замечаний (2 high, 3 medium) | 7 | 100 164 | ≈ 648 тыс. | 5 мин 12 с |
| 2 | 3 замечания (2 high, 1 medium) | 10 | 132 734 | ≈ 1.23 млн | 4 мин 05 с |
| 3 (сорвался) | ошибка запуска, см. §5 | 0 | — | — | 7 с |
| 3 | 1 замечание (medium) | 4 | 120 136 | ≈ 451 тыс. | 1 мин 49 с |
| 4 | 1 замечание (medium), отклонено осознанно | 5 | 116 545 | ≈ 559 тыс. | 1 мин 37 с |
| **Итого** | 10 замечаний: 9 исправлено, 1 отклонено | 26 | | **≈ 2.9 млн** | **≈ 12 мин 50 с** |

\* Сумма `total_usage_tokens` по всем шагам: примерно столько токенов модель прочитала,
большая часть — из кеша. Codex работает по вашей подписке ChatGPT, поэтому денежной стоимости у
него в журнале нет. Если внутри ревью Codex запускал собственных субагентов, их расход сюда не
вошёл: в журнале они не связаны с этими потоками.

### Jev (OpenRouter)

| Назначение | Вызовов | Результат |
|---|---:|---|
| Диагностика формата API | 3 | 2 × HTTP 400 (выяснял схему), 1 успешный, $0.00002016 |
| Маршрутизация подзадач (`choice`) | 5 | все p = 1.0 |
| Выбор skill (`choice`) | 3 | низкая уверенность (0.30–0.56), решал сам — см. §3 |
| Продукт: AI-оценка в smoke-прогоне | 4 | ≈ $0.0001 суммарно |
| **Итого** | **15** | **< $0.0003** |

### Я (Opus 5.5, оркестратор)

- Около 140 вызовов инструментов в основной работе: Bash/Write/Edit/Read, 5 запусков Agent,
  4 SendMessage, WebFetch/WebSearch, Chrome DevTools, 1 Skill. На этот отчёт ушло ещё около 10.
- Счётчик харнесса `total_tokens left` уменьшился с 15 000 000 до 14 607 950, то есть
  **≈ 392 тыс.** по этой метрике. Что именно она считает, харнесс не описывает. Судя по
  монотонному убыванию, это близко к объёму моего контекста, а не к оплаченным токенам: их
  больше, потому что контекст перечитывается на каждом шаге, в основном из кеша.

## 3. Решения Jev и следовал ли я им

| Вопрос | Ответ Jev | Что сделал |
|---|---|---|
| Каркас проекта | boilerplate-executor, p=1.0 | Следовал (агент №1) |
| Парсер/скоринг/хранилище/оркестрация | opus-self, p=1.0 | Следовал (писал сам) |
| Тесты и фикстуры | boilerplate-executor, p=1.0 | Следовал частично: тесты — агент №3, **фикстуры — сам**, потому что это знание разметки Upwork, от которого зависит корректность тестов |
| README EN/RU | boilerplate-executor, p=1.0 | Следовал (агент №2) |
| Прогон тестов | boilerplate-executor, p=1.0 | В основном следовал (прогоны делал агент №3). После правок по Codex 4 раза запускал `pytest -q` сам: вывод в 3 строки, а мне сразу нужно было решить, какие падения ожидаемы |
| Skill для реализации | subagent-driven-development, p=0.63, уверенность 0.56 | **Не следовал**: противоречит правилу проекта «Opus — основной исполнитель», уверенность низкая |
| Skill для ревью | code-review, p=0.45, уверенность 0.30 | **Не следовал**: вы явно потребовали Codex-ревью |
| Skill для HTML-отчёта | none 0.49 / frontend-design 0.46, уверенность 0.33 | Решил сам: загрузил `frontend-design`, так как отчёт — ежедневный интерфейс |

Глобальный `~/.claude/scripts/ojc/jev-route.sh` отправляет поле `prompt` вместо
`instructions` + `criteria` и получает HTTP 400. Скрипт я не трогал (он ваш, глобальный), а для
маршрутизации сделал исправленную копию во временной папке сессии.

## 4. Ход работы

1. **Разведка (≈5 мин).** Прочитал `CLAUDE.md`. Нашёл сломанный `jev-route.sh`, по документации
   OpenRouter и трём пробным вызовам выяснил реальный формат Jev API: `score` требует массив
   уровней, `choice` — объект, ответы в `answers.<key>`, текста модель не даёт.
2. **План и маршрутизация.** Показал план, задал Jev 5 вопросов о маршрутизации и 2 о выборе
   skill, агента №1 запустил в фоне.
3. **Ядро (я).** config → ids/textmatch/models → guards/discovery → parsing (переписал один раз
   после самопроверки: ложные ID из `data-ev-job-uid`, сравнение тегов bs4, «1 to 3 months» как
   отклики) → scoring/ai/storage → report/pipeline → browser/scraper/app.
4. **Проверки (я).** Фикстуры двух поколений разметки, прогон парсера. Сквозной smoke с
   фейковым браузером и живым Jev. Скриншоты отчёта: светлая и тёмная тема, 390 px. Настоящий
   Chrome: подсчёт карточек, прокрутка, «Load More Jobs». Два открытия upwork.com без входа
   показали Cloudflare-проверку «Один момент…», по ней доработал распознавание.
5. **Параллельно в фоне:** агент №2 (README), агент №3 (тесты), Codex раунд 1.
6. **Цикл ревью.** 4 раунда Codex. После каждого: исправление (я) → дозапуск агента №3 с
   тестами → обновление README (я) → следующий раунд. Тестов стало 207 → 240 → 246 → 248 → 249.

## 5. Что пошло не идеально

- **Сорвавшийся раунд Codex.** В текст фокуса ревью я вписал «…pytest -q», и утилита Codex
  прочитала `-m pytest` как свой флаг выбора модели («The 'pytest' model is not supported»).
  Перезапустил без флагов в тексте. Потеря — 7 секунд.
- **Недоверие агента №3.** Моё промежуточное сообщение ему (о новой фикстуре) он принял за
  возможную инъекцию и перепроверил всё по файлам на диске. Сообщение было от меня, но проверка
  правильная и ничего не стоила.
- **Рост контекста агента №3** до 369 тыс. из-за трёх дозапусков. Альтернатива — свежий агент
  на каждый дозапуск, но тогда он заново изучал бы код и тесты. Дозапуск с сохранённым
  контекстом оказался быстрее: 3–6 минут против 18 у первого запуска.
- **Мелкие правки собственного кода** до ревью: ложное срабатывание защиты на slug вакансии
  `/jobs/Captcha-challenge-…`, прерывание входа через Google SSO, гонка `page.content()` во
  время навигации. Всё нашёл сам до Codex.
- **Что Codex нашёл, а я пропустил**, по сути: обход обязательной остановки через исключение
  при навигации, запись чужой или удалённой страницы вакансии, небезопасный ключ AI-кеша, сырой
  HTML в debug-дампе, ранний выход из обнаружения поисков, миграция БД, совпадение заголовка как
  «доказательство» идентичности, потеря повторов и отложенных страниц.

## 6. Итог в цифрах

| Показатель | Значение |
|---|---|
| Время до финального отчёта | ≈ 1 ч 15 мин (19:19 → 20:34) |
| Код | 17 модулей `upwork_scout/` (+ `__init__`, `__main__`) и шаблон отчёта |
| Тесты | 249 passed (13 файлов тестов + `conftest.py`, 12 HTML-фикстур) |
| Замечания Codex | 10: 9 исправлено, 1 отклонено с обоснованием и закреплено тестом |
| Стоимость Jev | < $0.0003 |
| Живой авторизованный Upwork | не проверялся (нет доступа) |
