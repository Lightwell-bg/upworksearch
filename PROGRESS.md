# PROGRESS — upwork-scout

## План
1. [x] Каркас: .gitignore, requirements.txt, .env/.env.example, run.bat, venv, git init
2. [x] config.yaml + config.py
3. [x] Ядро: ids, textmatch, htmltext, models, guards, discovery, parsing
4. [x] scoring, storage, ai (Jev), report + шаблон, pipeline
5. [x] browser (Playwright), scraper (оркестрация прохода), app/__main__
6. [x] HTML-фикстуры + сквозной smoke (фейковый драйвер, живой Jev)
7. [x] Тесты tests/test_*.py — 249 passed
8. [x] README.md / README_RU.md
9. [x] Codex adversarial review — 4 раунда, замечания закрыты (одно отклонено осознанно)

## Codex-ревью
- Раунд 1 (5): эскалация CAPTCHA/логина после исключений; проверка страниц вакансий перед
  сохранением; отпечаток AI-кеша (модель, вопросы, пороги, версия; без даты публикации);
  санитизация debug-дампов; стабилизация списка поисков по набору ID. — исправлено.
- Раунд 2 (3): миграция схемы v1→v2; совпадение заголовка ≠ идентичность; повтор неудачных
  страниц вакансий при only_new. — исправлено.
- Раунд 3 (1): вакансии, не поместившиеся в max_per_run, терялись. — исправлено (details_pending).
- Раунд 4 (1): отложенная вакансия, выпавшая из ленты, не открывается. — отклонено осознанно:
  такие вакансии не попадают ни в один отчёт; флаг сохраняется, при повторном появлении
  страница открывается первой. Поведение закреплено тестом и описано в README.

## Живые проверки
- Playwright + установленный Chrome: подсчёт ссылок, прокрутка, «Load More Jobs», запрет
  навигации вне upwork.com — работают.
- upwork.com без входа (headless): Cloudflare-проверка с локализованным заголовком «Один момент…»
  распознаётся как security_check; по её структуре сделана фикстура cloudflare_live_ru.html.
- Авторизованный интерфейс Upwork НЕ проверялся (нет доступа).

## Решения
- Jev (OpenRouter `/api/v1/systemone`): score/choice/noul с `instructions`+`criteria`;
  объяснение собирается из вероятностей. Глобальный `~/.claude/scripts/ojc/jev-route.sh`
  шлёт `prompt` → HTTP 400 (не исправлялся: чужой глобальный скрипт).
- Этап входа — пассивное ожидание; после начала прохода CAPTCHA/логин → остановка прохода.

## Где остановился
- Задача завершена. Следующий шаг — первый живой запуск пользователем (см. README_RU.md).
